const color = "#ED5736";

const styles = {
  container: {
    width: "386px",
    height: "50px",
    backgroundColor: color,
    borderRadius: "5px",
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    cursor: "pointer"
  },

  label: {
    fontSize: "25px",
    color: "white"
  }
};

export default styles;
